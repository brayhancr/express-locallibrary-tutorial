extends layout

block content
  h1= title
  p Wellcome to #[em LocalLibrary], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else  
    p The library has the following record counts:

  ul
    li #[strong Books] !{'<span class=\'counter-tag\'>' + data.book_count + '</span>'}
    li #[strong Copies] !{'<span class=\'counter-tag\'>' + data.book_instance_count + '</span>'}
    li #[strong Copies available] !{'<span class=\'counter-tag\'>' + data.book_instance_available_count + '</span>'}
    li #[strong Authors] !{'<span class=\'counter-tag\'>' + data.author_count + '</span>'}
    li #[strong Genres] !{'<span class=\'counter-tag\'>' + data.genre_count + '</span>'}